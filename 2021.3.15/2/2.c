#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "queue.h"
node_t *start= NULL;
node_t *first;
node_t *last;

int main(void){
    int n = 1;
    while (n){
    printf("Enter a digit:\n");
    scanf("%d", &n);
    write(n);
}
while(read(&n))
printf("%d", n);
return 0;
}