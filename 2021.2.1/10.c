#include <stdio.h>
void  itob(int n,char s[], int b);
int main(){

 char *string = "test input";
 char to[16];
 copyStr(string,to);
 printf("\n%s",to);
 printf("\n%s",string);
 return 0;
}

void  itob(int n,char s[], int b){
      int i = 0;
    while (a > 0)
    {
        s[i] = a%2;
        a/=2;
        i++;
    }
   
    for (int j = i-1; j >= 0; j--)
    {
        printf("%d",arr[j]);
    }
}